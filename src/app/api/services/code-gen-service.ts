import type { CodeGenData, CodeChatActivationResponse } from '@/types'

// This service is now a placeholder. All code generation is handled via WebSocket in ai-chat.tsx.
class CodeGenService {
  // Optionally, you can add helpers for WebSocket URL construction if needed
}

export default new CodeGenService()